---
tags:
  - index
---

# Milestones

Development milestone summaries — what shipped, when, and where the detail lives.

- [[milestone-v0.0.0]] — P2P Development Foundation (2025-11-12): app skeleton (UI shell, IPC, RxDB, CRUD) + instrumented libp2p layer with `whtnxt://` handler, mDNS discovery, and the test peer.
- **v0.1.0 — pending**: the MVP baseline (`mvp` → `main` merge). Wave 1 (PRs #51–#55, 2026-08-01), wave 2 (five hardening epics), and the pre-merge touch-up are complete on `mvp`; awaiting human live QA and the merge PR. A milestone summary will be added when it lands.
