# Node
node_modules/
dist/
dist-electron/
release/
.npm/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS-specific
.DS_Store
Thumbs.db

# env
*.env

# ai
*.cursor*
*.vscode*
*.cursorrules
*.cursorignore

# agent-cycle scratch — process residue, never project source. The vault
# ("WhatNext - docs/") is the single source of truth: durable outcomes land
# there as reports/ADRs/specs and as PR records on merge. Only
# .claude/settings.json stays tracked.
.claude/worktrees/
.claude/cycle/
.claude/cycle-archive/

# local
TODO.md
QUESTIONS.md

# relay private key — never commit; generate fresh per deployment (see relay/README.md)
relay/relay-key.json
